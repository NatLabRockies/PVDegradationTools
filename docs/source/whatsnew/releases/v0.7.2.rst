v0.7.2 (2026-08-21)
===================

Enhancements
------------
- Efficient geospatial NSRDB loading. ``pvdeg.weather.get`` and
  ``pvdeg.weather.get_NSRDB`` (with ``geospatial=True``) now accept ``bbox``,
  ``downsample``, and ``land_only`` arguments and read only the requested sites
  instead of the full grid, making it practical to load very large NSRDB grids
  such as GOES ``full_disc`` (~9.5 million sites). The target site GIDs are
  resolved from a fast coordinate-only read via the new
  ``pvdeg.weather.nsrdb_gids``. (:pull:`351`)
- Added the ``"Polar"`` NSRDB satellite (the Arctic ``polar`` grid) to the
  satellite map, and a ``land_only`` option that drops offshore sites using the
  NSRDB ``offshore`` flag. (:pull:`351`)
- Added timeseries downsampling to the NSRDB geospatial loader through a
  ``resample`` argument, which block-averages the time axis with xarray
  ``coarsen`` to keep the dask graph small. (:pull:`351`)
- ``pvdeg.geospatial.analysis`` gained a ``gid_chunk`` argument and now
  auto-chunks the weather dataset along ``gid`` when it has a single chunk, so
  the analysis runs one task per location in parallel instead of serially on a
  single worker. (:pull:`351`)


Documentation
-------------
- Correct notebook contribution guidelines. Tutorial notebooks are committed **with**
  their executed outputs (validated by ``nbval``) rather than cleared, and no pre-commit
  hook strips outputs. Document running notebooks with
  ``jupyter nbconvert --to notebook --execute --inplace`` and marking non-reproducible
  cells (live API calls, figures, timestamps) with ``# NBVAL_IGNORE_OUTPUT``.
  (:issue:`352`, :pull:`353`)
- Add introductory paragraph to :doc:`api.rst </api>` (:issue:`358`, :pull:`359`)
- Rewrap module docstrings and comments to satisfy ``flake8``'s
  88-character line-length limit. (:pull:`364`)
- Fix Sphinx build warnings: correct short/mismatched heading underlines,
  remove duplicate ``autofunction`` object descriptions with ``:no-index:``,
  and clean up trailing whitespace across the user guide and API reference.
  (:pull:`364`)
- Exclude ``whatsnew/releases/*.rst`` from Sphinx source discovery, since
  these files are only meant to be pulled in via ``.. include::`` in
  ``whatsnew/index.rst`` and not built as standalone pages, fixing duplicate
  label and "not in any toctree" warnings. (:pull:`364`)
- Remove a large amount of dead/commented-out configuration from ``conf.py``
  (leftover boilerplate, disabled ``sphinx_gallery`` config, unused theme
  options) and update the copyright/organization info. (:pull:`364`)
- Reorganized the tutorials into numbered categories (``01_basics`` through
  ``06_advanced``, plus ``10_workshop_demos`` and ``tools``). Removed leftover
  duplicate notebooks and orphaned scripts left by the geospatial/scenario
  split, fixed the internal cross-reference links between tutorials, and
  updated the tutorial listing in the README and docs. (:pull:`351`)


Notebooks
---------
- Make the ASTM live demo and custom-functions tutorial notebooks reproducible
  offline by loading cached NSRDB TMY data (with ``USE_LIVE_*`` toggle for the
  live API path), fixing ``nbval`` failures caused by stale stored outputs and
  the PVGIS API being unreachable from CI. (:issue:`342`, :pull:`350`)
- Updated the geospatial tutorials (``tutorials/05_geospatial``) so the
  templates and outdoor LETID demonstrations run against the current API.
  (:pull:`351`)
- Added a ``tutorials/06_advanced/02_pysam_single_location`` tutorial
  demonstrating a single-location PySAM ``pvsamv1`` simulation from PVGIS
  weather. (:pull:`351`)
- The cached weather and scenario fixtures used by the tutorials now ship inside
  the package, in ``pvdeg/data/`` rather than ``tutorials/data/``, and the
  tutorials load them through ``pvdeg.DATA_DIR``. The previous paths resolved
  only from a repository clone, so the affected notebooks could not run against
  an installed ``pvdeg``. (:pull:`365`)


Deprecations
------------


Bug Fixes
---------
- Fixed a numerical blow-up in the LETID model at very low temperatures.
  ``pvdeg.letid.calc_letid_outdoors`` and ``pvdeg.letid.calc_letid_lab``
  integrate the three-state defect kinetics with an explicit forward-Euler step
  that could overshoot under extreme cold (e.g. Arctic / Siberian winter, module
  temperatures below about -60 °C), driving the defect-state populations far
  outside their physical range and collapsing the carrier lifetime (normalized
  power crashing to ~0.56). The state populations ``NA``, ``NB``, and ``NC`` are
  now clamped to ``[0, 100]`` at each step. (:pull:`351`)
- Fixed ``pvdeg.geospatial.analysis`` dropping the ``time`` dimension for
  functions that return a timeseries ``pandas.Series`` (for example
  ``pvdeg.temperature.cell``), which raised
  ``ValueError: Dimensions {'time'} missing on returned object``. The weather
  index name is now preserved when its dtype is coerced, and a ``Series`` result
  is named after the function's declared geospatial shape so that it matches the
  output template. (:pull:`351`)
- Fixed the Lambert projection in the geospatial map plots so that the data and
  the map features share a single set of axes and register correctly.
  (:pull:`351`)
- Continued the NREL to NLR migration: renamed the internal
  ``pvdeg.utilities.nrel_kestrel_check`` to ``pvdeg.utilities.nlr_kestrel_check``
  and updated the Kestrel hostname to the ``nlr.gov`` domain. (:pull:`351`)
- Fixed ``pvdeg.geospatial.analysis`` raising
  ``AttributeError: 'Future' object has no attribute 'loc'`` whenever a Dask
  client was active. ``analysis`` scatters ``meta_df`` to a broadcast
  ``distributed.Future`` to keep the task graph small, but xarray's
  ``map_blocks`` does not auto-resolve Futures passed through ``kwargs``;
  ``calc_block`` now materializes the Future before use. (:pull:`351`)
- Fixed ``pvdeg.utilities._add_material`` re-serializing the material database
  JSON files with escaped non-ASCII characters and no trailing newline, which
  produced spurious diffs in the packaged data files (for example
  ``pvdeg/data/O2permeation.json``). It now writes UTF-8 with a trailing
  newline. (:pull:`351`)
- Hardened the NSRDB geospatial subset loader. ``pvdeg.weather.ini_h5_geospatial``
  now derives its dask chunk spec per axis from the 2-D variables, where it was
  previously order-dependent and raised ``IndexError`` for any 1-D variable; site
  GIDs are de-duplicated before the h5 read, which otherwise failed h5py's
  strictly-increasing selection requirement; and an empty site selection raises a
  clear error from ``pvdeg.weather.nsrdb_gids`` rather than failing later inside
  the chunk arithmetic. (:pull:`365`)
- ``pvdeg.geospatial.analysis`` no longer warns when it successfully auto-chunks
  along ``gid``. The two warnings that call for user action -- a custom
  ``template`` supplied alongside ``gid_chunk``, and a custom ``template`` with a
  single ``gid`` chunk -- are unchanged. (:pull:`365`)
- Completed the NREL to NLR migration across documentation, metadata and service
  URLs: the ``developer``, ``nsrdb``, ``sam`` and ``maps`` hosts, the project's
  own repository links (``.zenodo.json``, ``CITATION.cff``, the Sphinx docs and
  ``tutorials/_config.yml``), and the HPC documentation links. (:pull:`365`)


Dependencies
------------
- Move ``pre-commit`` from core install requirements to a new optional
  development extra (``dev``), reducing the runtime dependency footprint.
  (:issue:`341`, :pull:`347`)
- Move ``jupytext`` from core install requirements to the ``dev`` extra, remove
  the unused ``bokeh`` dependency, and include ``dev`` in the ``all`` extra so
  the documented ``pip install -e .[all]`` contributor setup still provides the
  pre-commit tooling. (:pull:`364`, :pull:`365`)
- Bump aiohttp from 3.13.3 to 3.14.3. (:pull:`344`, :pull:`363`)
- Declare packaged data explicitly as ``"pvdeg" = ["data/*"]``, so the non-JSON
  data files are included in built distributions by declaration rather than as a
  side effect of ``MANIFEST.in``. Restrict ``authors`` to currently active
  maintainers. (:pull:`365`)



Contributors
------------
- Rajiv Daxini (:ghuser:`rdaxini`)
- Martin Springer (:ghuser:`martin-springer`)
- Kevin Anderson (:ghuser:`kandersolar`)
- Florian Kotthoff (:ghuser:`FlorianK13`)
